defmodule Db.Postgres.Schema do
  @moduledoc """
    What the data looks like

    see schema/*

    schemas are used to map external data into Elixir structs.
    We often use them to map database tables to Elixir data but they have many other
    use cases
  """
end
