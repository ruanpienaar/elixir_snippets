defmodule WorldClockWeb.DateTimeLive do
  # use Phoenix.LiveView

  # def render(_assigns) do

  #   # <%!-- <%= @date_time %> --%>

  #   ~H"""
  #   """
  # end

  # def mount(_params, _session, socket) do
  #   datetime_string = DateTime.to_string(DateTime.now!("Etc/UTC"))
  #   {:ok, assign(socket, :date_time, datetime_string)}
  # end

end
